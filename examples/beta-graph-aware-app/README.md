# Beta Graph-Aware App Example

This generic notes example demonstrates Propozito v0.2.0-beta artifacts without depending on any app-domain concepts from another repository.

It includes setup, terminology, project status, placeholder ledger, relationship graph, semantic memory index, implementation map, verification surface, signoff, prompt history, rework register, state lifecycle, security boundary, capability surface, evidence manifest, and context pack artifacts.

Run:

```sh
npm run validate:beta
```
